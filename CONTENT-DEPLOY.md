# نقل محتوى الموقع إلى الاستضافة

بعد إصلاح خطأ القالب (راجع `SERVER-FIX.md`) أصبح الموقع يفتح على الاستضافة، لكنه **فارغ**: مقالة «Hello world!» الافتراضية وصفحة «Sample Page»، بلا محتوى ولا تصميم.

هذا الدليل ينقل المحتوى والإعدادات من النسخة المحلية إلى `https://alkhaleejy-group.com/wordpress/`.

---

## 1) ما الذي سيُنقل

| المحتوى | العدد |
|---|---|
| الصفحات | 11 |
| الأخبار والمقالات | 4 |
| الأطباء | 6 |
| الخدمات الطبية | 6 |
| شركات التأمين | 5 |
| الإعلانات والتنبيهات | 1 |
| التخصصات | 6 |
| الصور والمرفقات | 20 (80 ملفًا مع المقاسات) |

> المحتوى الحالي **تجريبي** والصور مولّدة آليًا — يُستبدل بالمحتوى الحقيقي قبل الإطلاق.

---

## 2) الحزم الجاهزة في مجلد `dist\`

| الملف | ماذا يحتوي |
|---|---|
| `clinic-custom.zip` | القالب الابن `astra-clinic` + إضافة `clinic-core` |
| `clinic-uploads.zip` | صور ومرفقات الموقع (80 ملفًا) |
| `clinic-content.xml` | المحتوى كاملًا بصيغة WordPress (WXR) |
| `clinic-server-setup.zip` | إضافة إعداد تعمل **لمرة واحدة** |

> روابط الصور داخل `clinic-content.xml` مكتوبة بعنوان الاستضافة `https://alkhaleejy-group.com/wordpress` وليس `localhost` — لهذا **يجب رفع الصور قبل استيراد المحتوى**، وإلا فشل الاستيراد في جلبها.

لإعادة بناء الحزم بعد أي تعديل محلي:

```cmd
php build\make-zips.php
```

---

## 3) الترتيب — نفّذه كما هو

الترتيب مهم: الصور قبل المحتوى، والمحتوى قبل إضافة الإعداد.

### الخطوة 1 — تأكد أن القالب الابن مفعّل

**المظهر ← القوالب** ← يجب أن يكون **`Astra Clinic`** هو المفعّل (وليس `Astra`).

### الخطوة 2 — ضبط لغة الموقع

**الإعدادات ← عام ← لغة الموقع** ← اختر **العربية** ← حفظ.

ينزّل ووردبريس ملفات اللغة ويحوّل لوحة التحكم والموقع إلى الاتجاه من اليمين لليسار (RTL). بدون هذه الخطوة يبقى التصميم بالاتجاه الإنجليزي.

### الخطوة 3 — رفع الصور

في cPanel ← **File Manager** ← ادخل:

```
public_html/wordpress/wp-content
```

ارفع `clinic-uploads.zip` ← زر يمين ← **Extract** ← ثم احذف ملف الـ ZIP.

ينتج عن ذلك `wp-content/uploads/2026/08/...`.

> إذا كان مجلد `uploads` موجودًا فيه ملفات، لن يُحذف شيء — تُضاف الملفات الجديدة فقط.

### الخطوة 4 — استيراد المحتوى

**أدوات ← استيراد ← WordPress** ← «تثبيت الآن» إن طُلب ← **تشغيل المستورد**.

1. ارفع `dist\clinic-content.xml`
2. في شاشة «تعيين الكتّاب»: اختر تعيين كل المحتوى إلى المستخدم `superuser`
3. ✅ فعّل خيار **«تنزيل واستيراد المرفقات»** (Download and import file attachments)
4. اضغط **إرسال**

### الخطوة 5 — تشغيل إضافة الإعداد

ملف الاستيراد ينقل المحتوى فقط — لا ينقل الألوان ولا الخطوط ولا الصفحة الرئيسية ولا القائمة ولا نموذج الحجز. هذه الإضافة تضبطها كلها دفعة واحدة.

**الإضافات ← أضف جديد ← ارفع إضافة** ← `dist\clinic-server-setup.zip` ← **تثبيت** ← **تفعيل**.

عند التفعيل تظهر رسالة خضراء بما تم:

- ألوان وخطوط Astra (كحلي `#1F2B6C` + أزرق `#159EEC` + خط Cairo)
- الصفحة الرئيسية ← «الرئيسية»، وصفحة المقالات ← «الأخبار والمقالات»
- ربط «القائمة الرئيسية» بموقع القائمة الأساسي
- الروابط الدائمة ← «اسم المقالة» + إعادة بناء قواعد التوجيه
- إنشاء نموذج «طلب حجز موعد» في Fluent Forms
- بيانات تواصل تجريبية (لا تُلمس إن كانت مملوءة مسبقًا)

**بعد ظهور الرسالة: عطّل الإضافة واحذفها** — مهمتها انتهت.

> إذا ظهر تنبيه «لم يُعثر على صفحة الرابط home» فهذا يعني أن الاستيراد (الخطوة 4) لم ينجح — أعد الخطوة 4 ثم أعد تفعيل الإضافة.
>
> وإذا ظهر «إضافة Fluent Forms غير مفعّلة» — فعّلها من «الإضافات» ثم عطّل إضافة الإعداد وأعد تفعيلها.

### الخطوة 6 — حذف المحتوى الافتراضي

- **المقالات** ← احذف «Hello world!»
- **الصفحات** ← احذف **صفحتين**: «Sample Page» (كانت على الخادم) و«مثال على صفحة» (جاءت مع الاستيراد — صفحة افتراضية من النسخة المحلية)
- **التعليقات** ← احذف تعليق «A WordPress Commenter»

> عدد الصفحات بعد التنظيف يجب أن يكون **10**.

---

## 4) قائمة التحقق

- [ ] الصفحة الرئيسية تعرض قسم الهيرو الكحلي وليس قائمة مقالات
- [ ] اتجاه الصفحة من اليمين لليسار وخط Cairo ظاهر
- [ ] القائمة العلوية تعرض 9 عناصر: الرئيسية / عن المستوصف / التخصصات / الخدمات / الأطباء / الأخبار / التأمينات / الأسئلة الشائعة / اتصل بنا
- [ ] صفحة «الأطباء» تعرض 6 أطباء بصورهم
- [ ] صفحة «الخدمات الطبية» تعرض 6 خدمات
- [ ] صفحة «التأمينات المعتمدة» تعرض 5 شركات
- [ ] صفحة «احجز موعدًا» تعرض النموذج فعليًا (وليس النص `[fluentform id="..."]`)
- [ ] شريط التنبيه الكحلي يظهر أعلى الصفحات
- [ ] زر الواتساب الثابت يظهر في الزاوية
- [ ] فتح صفحة طبيب مفرد يعمل بدون خطأ 404

> إذا ظهر خطأ **404** في صفحات الأطباء أو الخدمات: **الإعدادات ← الروابط الدائمة ← حفظ** (بدون تغيير شيء) لإعادة بناء قواعد التوجيه.

---

## 5) بعد النقل — قبل الإطلاق

- [ ] تغيير كلمة مرور `superuser` دوريًا (الحالية في `credentials.local.md` — خارج المستودع)
- [ ] إدخال بيانات التواصل الحقيقية في **«إعدادات المستوصف»** بدل التجريبية
- [ ] استبدال الصور التجريبية بصور حقيقية للأطباء والخدمات
- [ ] استبدال نصوص المحتوى التجريبي
- [ ] تفعيل شهادة SSL وتحديث عنوان الموقع إلى `https://`
- [ ] ضبط SMTP لتصل إشعارات نموذج الحجز بالبريد
- [ ] أخذ نسخة احتياطية من `wp-content/` وقاعدة البيانات

---

## 6) ملاحظة عن النسخة المحلية

النسخة المحلية لم تتغير — ما زالت تعمل على `http://localhost:8080`. أُعيد ضبط عناوينها بعد التصدير.

`start-site.cmd` و `wp.cmd` يشيران إلى `tools\php\php.exe` وهو **غير موجود حاليًا**. لتشغيل الموقع محليًا استخدم PHP المثبّت على الجهاز:

```cmd
"C:\PHP\php-8.4.21-nts\php.exe" -S localhost:8080 -t site router.php
```

ولأوامر WP-CLI:

```cmd
"C:\PHP\php-8.4.21-nts\php.exe" tools\wp-cli\wp-cli.phar --path=site <command>
```
