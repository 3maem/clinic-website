# نشر موقع Alkhaleejy Medical Company

هذا الفرع (`deploy`) **ليس مستودع كود** — محتواه هو بالضبط ما يجب أن يكون داخل `public_html` على الاستضافة: ووردبريس 7.0.4 عربي كامل + القالب والإضافات + الصور + قاعدة البيانات.

> فرع `main` هو مصدر الكود وأدوات التطوير المحلية. **لا تنشر من `main`** — بنيته مختلفة تمامًا (ووردبريس فيه داخل `site/`)، ولو نسخته إلى `public_html` سيتعطّل الموقع.

---

## ما يحتويه هذا الفرع

| | |
|---|---|
| نواة ووردبريس | 7.0.4 عربي |
| القالب | Astra + القالب الابن `astra-clinic` |
| الإضافات | ACF · Fluent Forms · Spectra · **إدارة المستوصف** (`clinic-core`) |
| اللغة | ملفات الترجمة العربية جاهزة (RTL) |
| الصور | `wp-content/uploads` |
| قاعدة البيانات | `deploy-db/clinic-db.sql` |

**ما لا يحتويه عمدًا:**

- **`wp-config.php`** — فيه اسم قاعدة البيانات ومستخدمها وكلمة مرورها والمفاتيح الأمنية. هذا الملف خاص بالخادم ولا يُوضع في مستودع. تنسخه من التثبيت الحالي (الخطوة 2).
- **`sqlite-database-integration` و `wp-content/db.php`** — للتشغيل المحلي فقط، ووجودهما على الاستضافة يكسر الاتصال بقاعدة البيانات.

---

## الخطوات

> **قبل أي شيء**: cPanel ← **Backup** ← خذ نسخة من `public_html` وقاعدة البيانات. الاستيراد يمسح الجداول الحالية.

### 1) أنزل الملفات

cPanel ← **Git™ Version Control** ← Create:

| الحقل | القيمة |
|---|---|
| Clone URL | `https://github.com/3maem/clinic-website.git` |
| Repository Path | `/home/<حسابك>/clinic-deploy` |

ثم من **Manage** ← تبويب **Pull or Deploy** ← بدّل الفرع إلى `deploy`.

> إن لم تتوفر الأداة: نزّل الفرع كـ ZIP من GitHub (Code ← Download ZIP بعد اختيار فرع `deploy`) وارفعه عبر File Manager وفكّه.

**لا تنسخ الملفات إلى `public_html` بعد** — الخطوة التالية أولًا.

### 2) احتفظ بـ `wp-config.php`

من File Manager (فعّل **Settings ← Show Hidden Files**):

انسخ `public_html/wp-config.php` إلى مكان آمن مؤقتًا — فيه بيانات الاتصال بقاعدة البيانات، وبدونه لن يعمل الموقع.

### 3) استبدل محتوى `public_html`

1. احذف كل ما في `public_html` **عدا**: `.well-known` و `morasalaty-alkhaleejy-group.com` وأي مجلد لموقع آخر
2. انسخ كل محتوى `clinic-deploy` (بما فيه الملفات المخفية مثل `.htaccess`) إلى `public_html`
3. أعد `wp-config.php` إلى `public_html`

### 4) استورد قاعدة البيانات

cPanel ← **phpMyAdmin** ← اختر القاعدة المذكورة في `wp-config.php` عند `DB_NAME` ← تبويب **Import** ← اختر `deploy-db/clinic-db.sql` ← **Go**

الملف يحذف الجداول القديمة وينشئها بنفسه — لا حاجة لحذف يدوي.

> **خطأ `Unknown collation: utf8mb4_unicode_520_ci`** ← استورد `clinic-db-compat.sql` بدلًا منه.
>
> **خطأ حجم الملف** ← اضغط الملف بصيغة `.zip` وارفعه مضغوطًا؛ phpMyAdmin يفكّه تلقائيًا.

### 5) احذف مجلد `deploy-db` من `public_html`

مهمته انتهت، ولا داعي لبقاء نسخة من قاعدة البيانات على الخادم.

### 6) افتح الموقع

<https://alkhaleejy-group.com/> — يجب أن يظهر كاملًا: عربي، من اليمين لليسار، بالألوان والمحتوى.

---

## قائمة التحقق

- [ ] الصفحة الرئيسية تعرض قسم الهيرو الكحلي (وليس قائمة مقالات)
- [ ] الاتجاه من اليمين لليسار وخط Cairo ظاهر
- [ ] القائمة العلوية 9 عناصر
- [ ] `/doctors-page/` ← 6 أطباء بصورهم
- [ ] `/services-page/` ← 6 خدمات
- [ ] `/insurance/` ← 5 شركات تأمين
- [ ] `/book-appointment/` ← النموذج يظهر فعليًا (وليس النص `[fluentform id="3"]`)
- [ ] شريط التنبيه الكحلي + زر الواتساب في الزاوية
- [ ] `/doctors/<اسم-الطبيب>/` يفتح بدون 404

> **404 في صفحات الأطباء أو الخدمات** ← الإعدادات ← الروابط الدائمة ← **حفظ** (بدون تغيير شيء).

### بعد التحقق مباشرة

- [ ] **غيّر كلمة مرور المستخدم `superuser`** — الاستيراد يعيدها إلى القيمة المضمّنة في نسخة قاعدة البيانات
- [ ] راجع بريد المدير: `abdul.manan@alkhalejy-group.com` — لاحظ أن نطاقه يختلف عن نطاق الموقع بحرف `e`. إن كان خطأ مطبعيًا فلن تصل رسائل استعادة كلمة المرور

---

## تحديث الموقع لاحقًا

التعديلات تتم **محليًا** ثم تُدفع هنا — لا تعدّل ملفات الخادم مباشرة، لأن أي `pull` لاحق سيطمسها.

بعد أي تعديل محلي، من جذر مشروع التطوير:

```cmd
"C:\PHP\php-8.4.21-nts\php.exe" build\make-db.php
"C:\PHP\php-8.4.21-nts\php.exe" build\make-deploy-branch.php
```

ثم على الخادم: cPanel ← Git™ Version Control ← **Update from Remote** ← **Deploy HEAD Commit**، وأعد استيراد قاعدة البيانات إن تغيّر المحتوى.
