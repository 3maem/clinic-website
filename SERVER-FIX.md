# إصلاح خطأ الخادم: `Call to undefined function astra_html_before()`

دليل تنفيذي لما يجب عمله **على الاستضافة** (وليس في هذا المستودع).

---

## 1) الخطأ وسببه

```
Fatal error: Uncaught Error: Call to undefined function astra_html_before()
in /home/d5db7d0bh06i/public_html/wordpress/wp-content/themes/astra/header.php:18
```

الدالة `astra_html_before()` معرّفة في:

```
themes/astra/inc/core/theme-hooks.php   (السطر 96)
```

ويُحمّل هذا الملف من:

```
themes/astra/functions.php              (السطر 94)
```

الخطأ يعني أن **ملفات القالب تعمل لكن `functions.php` الخاص بقالب Astra لم يُنفَّذ إطلاقًا** — لذلك كل دوال `astra_*` غير معرّفة. المشكلة في **نسخة قالب Astra المرفوعة على الخادم**، وليست في كود المستوصف (`clinic-core` أو `astra-clinic`).

### الأسباب المرجّحة (بالترتيب)

| # | السبب | لماذا يحدث |
|---|---|---|
| 1 | **رفع ناقص للقالب** | قالب Astra يحتوي **746 ملفًا / 24 ميجابايت**. الرفع عبر FTP ملفًا ملفًا أو فك ضغط متقطع في cPanel يُسقط ملفات أو يقطعها — فيختفي `functions.php` أو `inc/core/theme-hooks.php` أو يصبح حجمه صفرًا. |
| 2 | **صلاحيات ملف خاطئة** | إذا كان `functions.php` غير قابل للقراءة، يفشل تضمينه بتحذير مخفي (`display_errors` مغلق) ويكمل التنفيذ حتى ينهار عند أول دالة `astra_*`. |
| 3 | **تحديث نصف مكتمل** | ملفات جديدة فوق مجلد `inc/` قديم أو ناقص. |

### ملاحظة مهمة من أثر الخطأ (Stack Trace)

المسار في الخطأ يمرّ عبر `themes/astra/index.php` — أي أن **القالب المفعّل على الخادم هو `astra` الأصلي وليس القالب الابن `astra-clinic`**. هذا يعني أن ألوان MEDDICAL وتنسيقات RTL وكود القالب الابن **غير مفعّلة على الاستضافة**، وغالبًا إضافة `clinic-core` غير مرفوعة أيضًا. راجع القسم (4) و(5).

---

## 2) إعادة الموقع للعمل فورًا (إجراء طوارئ)

إذا كانت لوحة التحكم `/wp-admin` منهارة أيضًا ولا يمكن الدخول:

```bash
cd ~/public_html/wordpress/wp-content/themes
mv astra astra.broken
```

عند فقد القالب المفعّل يرجع ووردبريس تلقائيًا إلى قالب افتراضي (`twentytwenty-*`)، فيعود الموقع ولوحة التحكم للعمل بشكل مؤقت وبدون تنسيق. ثم أكمل الإصلاح في القسم (3).

> لا تحذف `astra.broken` قبل نجاح التثبيت الجديد.

---

## 3) الإصلاح: إعادة تثبيت قالب Astra من الصفر

**القاعدة الذهبية: ارفع ملف ZIP واحدًا وفكّ ضغطه على الخادم. لا ترفع المجلد مفكوكًا عبر FTP.**

### الطريقة (أ) — عبر SSH / Terminal في cPanel *(الأفضل)*

```bash
cd ~/public_html/wordpress/wp-content/themes

# 1) عزل النسخة التالفة
mv astra astra.broken

# 2) تنزيل النسخة الرسمية (نفس نسخة البيئة المحلية المجرّبة)
curl -LO https://downloads.wordpress.org/theme/astra.4.13.9.zip

# 3) فك الضغط على الخادم مباشرة
unzip -q astra.4.13.9.zip

# 4) ضبط الصلاحيات
find astra -type d -exec chmod 755 {} \;
find astra -type f -exec chmod 644 {} \;

# 5) تنظيف
rm astra.4.13.9.zip
```

### الطريقة (ب) — عبر لوحة تحكم ووردبريس *(لا تحتاج SSH)*

إذا كانت لوحة التحكم تعمل (بعد إجراء الطوارئ في القسم 2):

**المظهر ← القوالب ← أضف جديد ← ابحث عن "Astra" ← تثبيت**

هذه أنظف طريقة — ووردبريس ينزّل الملف ويفك ضغطه على الخادم بنفسه.

### الطريقة (ج) — عبر مدير الملفات في cPanel

1. نزّل على جهازك: `https://downloads.wordpress.org/theme/astra.4.13.9.zip`
2. في cPanel ← **File Manager** ← ادخل `public_html/wordpress/wp-content/themes`
3. أعد تسمية `astra` إلى `astra.broken`
4. **Upload** ← ارفع ملف الـ ZIP كما هو
5. زر يمين على الملف ← **Extract**
6. احذف ملف الـ ZIP بعد فك الضغط

---

## 4) رفع القالب الابن والإضافة المخصصة

الملفات المخصصة للمستوصف جاهزة في هذا المستودع:

```
dist\clinic-custom.zip     (11 كيلوبايت — 9 ملفات)
```

محتواه:

```
themes/astra-clinic/       القالب الابن (ألوان MEDDICAL + Cairo + RTL)
plugins/clinic-core/       إضافة «إدارة المستوصف» (أنواع المحتوى + الشورت كودات + الإعدادات)
```

### طريقة الرفع

ارفع `clinic-custom.zip` إلى مجلد **`wp-content`** على الخادم وفكّ ضغطه هناك:

```bash
cd ~/public_html/wordpress/wp-content
# ارفع clinic-custom.zip إلى هذا المجلد ثم:
unzip -o clinic-custom.zip
rm clinic-custom.zip

find themes/astra-clinic plugins/clinic-core -type d -exec chmod 755 {} \;
find themes/astra-clinic plugins/clinic-core -type f -exec chmod 644 {} \;
```

سيوزّع الملفات تلقائيًا إلى `wp-content/themes/astra-clinic` و`wp-content/plugins/clinic-core`.

> بديل: في لوحة التحكم يمكن رفع كل مجلد على حدة كملف ZIP مستقل عبر «المظهر ← القوالب ← أضف جديد ← ارفع» و«الإضافات ← أضف جديد ← ارفع».
>
> مجلد `dist\` غير متتبَّع في Git (ملف ناتج). إذا لم تجد `clinic-custom.zip` بعد استنساخ المستودع، أنشئه بضغط المجلدين `site\wp-content\themes\astra-clinic` و`site\wp-content\plugins\clinic-core` بنفس البنية أعلاه (`themes/…` و`plugins/…`).

---

## 5) الإضافات المطلوبة وتفعيل كل شيء

### الإضافات (من «الإضافات ← أضف جديد» — النسخ المجرّبة محليًا)

| الإضافة | المعرّف في مستودع ووردبريس | النسخة المجرّبة |
|---|---|---|
| Spectra – Gutenberg Blocks | `ultimate-addons-for-gutenberg` | 2.20.1 |
| Advanced Custom Fields (ACF) | `advanced-custom-fields` | 6.8.7 |
| Fluent Forms | `fluentform` | 6.2.12 |
| **إدارة المستوصف** (مخصصة) | من `clinic-custom.zip` | 1.0.0 |

> ⚠️ **لا تُثبّت `sqlite-database-integration` على الاستضافة.** هي مخصصة للتشغيل المحلي بدون MySQL فقط. الاستضافة تستخدم MySQL، وتثبيتها هناك سيكسر الاتصال بقاعدة البيانات.

### ترتيب التفعيل

1. **الإضافات ← فعّل**: ACF، ثم Spectra، ثم Fluent Forms، ثم «إدارة المستوصف».
2. **المظهر ← القوالب ← فعّل `Astra Clinic`** (القالب الابن، وليس `Astra`).

> ترتيب التفعيل نفسه لا يهم تقنيًا (`clinic-core` يسجّل حقوله على خطاف `acf/init` مع فحص وقائي)، **لكن ACF يجب أن تبقى مفعّلة** — بدونها لن تظهر صناديق «بيانات الطبيب» و«بيانات الخدمة» و«بيانات التأمين» في شاشات التحرير، دون أي رسالة خطأ.

### بعد التفعيل

- **الإعدادات ← الروابط الدائمة** ← اختر **«اسم المقالة»** ثم **حفظ** (يعيد بناء قواعد التوجيه — ضروري لصفحات الأطباء والخدمات).
- **إعدادات المستوصف** ← عبّئ أرقام الهاتف والواتساب والبريد والعنوان ورابط الخريطة وأوقات العمل.

---

## 6) قائمة التحقق النهائية

نفّذ هذه الفحوصات على الخادم بعد الإصلاح:

```bash
cd ~/public_html/wordpress/wp-content/themes/astra

# يجب أن يظهر الملفان بحجم غير صفري
ls -la functions.php inc/core/theme-hooks.php

# يجب أن يكون العدد ≈ 746
find . -type f | wc -l

# يجب أن تظهر نتيجة واحدة
grep -c "function astra_html_before" inc/core/theme-hooks.php
```

ثم من المتصفح:

- [ ] الصفحة الرئيسية تفتح بدون خطأ
- [ ] `/wp-admin` يفتح ويظهر في القائمة الجانبية: **الأطباء / الخدمات الطبية / شركات التأمين / إعدادات المستوصف**
- [ ] القالب المفعّل في «المظهر ← القوالب» هو **Astra Clinic**
- [ ] الألوان كحلي `#1F2B6C` وأزرق `#159EEC` وخط Cairo ظاهرة، واتجاه الصفحة من اليمين لليسار
- [ ] صفحة «احجز موعدًا» تعرض النموذج (وليس النص `[fluentform id="3"]` كما هو)

---

## 7) أخطاء شائعة يجب تجنبها

| ❌ لا تفعل | ✅ افعل |
|---|---|
| رفع مجلد `astra` مفكوكًا عبر FTP (746 ملفًا — هذا سبب المشكلة الأصلي غالبًا) | ارفع ZIP واحدًا وفكّ ضغطه على الخادم |
| نسخ ملفات مفردة فوق تثبيت قديم | احذف/اعزل المجلد كاملًا ثم ثبّت نسخة نظيفة |
| تفعيل قالب `Astra` الأصلي | فعّل القالب الابن `Astra Clinic` |
| تثبيت `sqlite-database-integration` على الاستضافة | اتركها للتشغيل المحلي فقط |
| تعديل ملفات `themes/astra/` مباشرة | كل التخصيصات تذهب في `astra-clinic` أو `clinic-core` |

---

## 8) نقل المحتوى (إن كان الخادم فارغًا من المحتوى)

القالب والإضافات لا تنقل المحتوى. لنقل الأطباء والخدمات والأخبار والصفحات من النسخة المحلية:

1. محليًا: **أدوات ← تصدير ← كل المحتوى** → ملف WXR بصيغة XML.
2. على الخادم: **أدوات ← استيراد ← WordPress** (ثبّت أداة الاستيراد إن طُلب) → ارفع ملف XML → فعّل خيار **«تنزيل واستيراد المرفقات»**.
3. بعدها: **الإعدادات ← القراءة** ← اجعل الصفحة الرئيسية صفحة ثابتة = «الرئيسية»، وصفحة المقالات = «الأخبار والمقالات».
4. **المظهر ← القوائم** ← اربط «القائمة الرئيسية» بموقع القائمة الأساسي في Astra.

> المحتوى الحالي في النسخة المحلية **تجريبي** — يُستبدل بالمحتوى الحقيقي قبل الإطلاق.

---

## 9) قبل الإطلاق

- [ ] تغيير كلمة مرور `superuser` دوريًا (الحالية في `credentials.local.md` — خارج المستودع)
- [ ] تفعيل شهادة SSL وتحديث عنوان الموقع إلى `https://`
- [ ] ضبط SMTP لتصل إشعارات نموذج الحجز بالبريد
- [ ] إدخال بيانات التواصل الحقيقية في «إعدادات المستوصف»
- [ ] أخذ نسخة احتياطية من `wp-content/` وقاعدة البيانات
