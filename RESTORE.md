# استعادة الموقع على الاستضافة — نسخة طبق الأصل

دليل رفع الموقع كاملًا إلى `https://alkhaleejy-group.com` بخطوتين، بدل الاستيراد اليدوي في `CONTENT-DEPLOY.md`.

النتيجة: الموقع على الاستضافة يصير **مطابقًا تمامًا** للنسخة المحلية — المحتوى والألوان والقائمة والصفحة الرئيسية ونموذج الحجز وإعدادات المستوصف، بدون أي ضبط يدوي بعد الرفع.

> **آخر تحديث**: 2026-08-19 — بعد أن مُسحت إعدادات الاستضافة وأُعيد تثبيت ووردبريس على جذر الدومين.

---

## 1) لماذا هذه الطريقة

الاستيراد اليدوي (XML + إضافة الإعداد) سبع خطوات، وكل خطوة قابلة للفشل، ولا ينقل كل شيء. هذه الطريقة تنقل **قاعدة البيانات نفسها** — يعني كل إعداد بلا استثناء.

العقبة الوحيدة كانت أن النسخة المحلية تعمل على **SQLite** والاستضافة على **MySQL**. الحل: نسخة محلية ثانية على MySQL في `site-mysql/` تُبنى من نفس بيانات `site/`.

| | `site/` | `site-mysql/` |
|---|---|---|
| قاعدة البيانات | SQLite (`.ht.sqlite`) | MariaDB على المنفذ 3307 |
| الغرض | التطوير اليومي | مصدر حزم النشر |
| في Git | متتبَّع جزئيًا | مستثنى بالكامل |

---

## 2) وضع الاستضافة الحالي (تحقّق منه قبل البدء)

| | القيمة |
|---|---|
| العنوان | `https://alkhaleejy-group.com/` — **جذر الدومين**، وليس `/wordpress/` |
| مجلد الملفات | `public_html/` |
| ووردبريس | 7.0.4 |
| PHP | 8.2.32 |

> المسار القديم `/wordpress/` لم يعد موجودًا (يرجع 404). كل الحزم في `dist/` مبنية على العنوان الجديد.

---

## 3) الملفات الجاهزة في `dist\`

| الملف | الحجم | المحتوى |
|---|---|---|
| `clinic-wp-content.zip` | 36 ميجابايت | مجلد `wp-content` كاملًا: 3560 ملفًا — قالب Astra + القالب الابن + الإضافات الأربع + ملفات اللغة العربية + الصور |
| `clinic-db.sql` | 290 كيلوبايت | قاعدة البيانات كاملة، الروابط فيها مضبوطة مسبقًا على `https://alkhaleejy-group.com` |
| `clinic-db-compat.sql` | 290 كيلوبايت | نسخة احتياطية بترميز `utf8mb4_unicode_ci` — تُستخدم فقط إذا رفض السيرفر الترميز الأساسي |

الملفات الثلاثة **مستثناة من Git** (الحجم + تجزئة كلمة مرور المدير). تُبنى محليًا بأمر واحد — راجع القسم (6).

**بيانات الموقع المضمّنة في `clinic-db.sql`:**

| | |
|---|---|
| اسم الموقع | Alkhaleejy Medical Company |
| العنوان | `https://alkhaleejy-group.com` |
| المستخدم | `superuser` |
| البريد | `abdul.manan@alkhalejy-group.com` |
| كلمة المرور | في `credentials.local.md` — خارج المستودع |

**ما لا يحتويه `clinic-wp-content.zip` عمدًا:**

- قوالب ووردبريس الافتراضية (`twentytwenty*`) — موجودة على الاستضافة أصلًا
- إضافة `sqlite-database-integration` وملف `wp-content/db.php` — **تكسر الاتصال بقاعدة البيانات على الاستضافة**

---

## 4) خطوات الرفع

> **قبل أي شيء**: من cPanel ← **Backup** خذ نسخة احتياطية من `public_html` وقاعدة البيانات. الاستيراد يمسح الجداول الحالية.

### الخطوة 1 — رفع الملفات

1. cPanel ← **File Manager** ← ادخل `public_html`
2. **Upload** ← ارفع `clinic-wp-content.zip`
3. زر يمين على الملف ← **Extract** ← في نفس المجلد (`public_html`)
4. عند سؤال الاستبدال: **نعم، استبدل الكل**
5. احذف ملف الـ ZIP بعد فك الضغط

ينتج عن ذلك `public_html/wp-content/themes/...` و`plugins/...` و`languages/...` و`uploads/...`

### الخطوة 2 — استيراد قاعدة البيانات

1. cPanel ← **phpMyAdmin**
2. اختر قاعدة بيانات الموقع من القائمة اليمنى (اسمها في `public_html/wp-config.php` عند `DB_NAME`)
3. تبويب **Import** ← **Choose File** ← `clinic-db.sql` ← **Go**

الملف يحذف الجداول القديمة وينشئها من جديد بنفسه — لا حاجة لحذف يدوي.

> **لو ظهر خطأ `Unknown collation: utf8mb4_unicode_520_ci`**: استورد `clinic-db-compat.sql` بدلًا منه. هذا كل الفرق بين الملفين.
>
> **لو ظهر خطأ حجم الملف**: اضغط `clinic-db.sql` بصيغة `.zip` وارفعه مضغوطًا — phpMyAdmin يفك الضغط تلقائيًا.

### الخطوة 3 — تحقّق

افتح `https://alkhaleejy-group.com/` — يجب أن يظهر الموقع كاملًا فورًا: عربي، من اليمين لليسار، بالألوان والمحتوى.

---

## 5) قائمة التحقق بعد الرفع

- [ ] الصفحة الرئيسية تعرض قسم الهيرو الكحلي (وليس قائمة مقالات)
- [ ] اتجاه الصفحة من اليمين لليسار وخط Cairo ظاهر
- [ ] القائمة العلوية تعرض 9 عناصر
- [ ] `/doctors-page/` تعرض 6 أطباء بصورهم
- [ ] `/services-page/` تعرض 6 خدمات
- [ ] `/insurance/` تعرض 5 شركات تأمين
- [ ] `/book-appointment/` تعرض النموذج فعليًا (وليس النص `[fluentform id="3"]`)
- [ ] شريط التنبيه الكحلي أعلى الصفحات + زر الواتساب في الزاوية
- [ ] فتح صفحة طبيب مفرد (`/doctors/<اسم-الطبيب>/`) يعمل بدون خطأ 404

> **خطأ 404 في صفحات الأطباء أو الخدمات**: الإعدادات ← الروابط الدائمة ← **حفظ** (بدون تغيير شيء). هذا يعيد بناء ملف `.htaccess` الذي لا تنقله قاعدة البيانات.

### فورًا بعد التحقق — أمني

- [ ] سجّل الدخول بالمستخدم `superuser` (كلمة المرور في `credentials.local.md` خارج المستودع) وتأكد أن لوحة التحكم تفتح
- [ ] احذف إضافة `astra-sites` إن لم تكن مستخدمة (تعطّلت تلقائيًا بعد الاستيراد)
- [ ] تأكد أن SSL يعمل وأن العنوان `https://`

---

## 6) إعادة بناء الحزم بعد أي تعديل محلي

عدّل على `site-mysql` (على `http://localhost:8080`)، ثم:

```cmd
REM 1) شغّل MariaDB (اتركه مفتوحًا)
"C:\xampp\mysql\bin\mysqld.exe" --defaults-file=C:\xampp\mysql\bin\my.ini --port=3307 --console

REM 2) شغّل الموقع المحلي على http://localhost:8080 (اتركه مفتوحًا)
"C:\PHP\php-8.4.21-nts\php.exe" -S localhost:8080 -t site-mysql router-mysql.php

REM 3) ابنِ الحزمتين بعد ما تخلّص التعديل
"C:\PHP\php-8.4.21-nts\php.exe" build\make-db.php
"C:\PHP\php-8.4.21-nts\php.exe" build\make-deploy.php
```

`build\make-db.php` يتكفّل بكل التفاصيل الدقيقة: استبدال الروابط داخل البيانات المتسلسلة، وتصحيح خيار `uagb_site_url` الذي تخزّن فيه إضافة Spectra اسم النطاق منفصلًا، وإضافة ترويسة `SET NAMES` و`sql_mode` التي تمنع فشل الاستيراد، وبناء نسخة `compat`. ويفشل بصوتٍ عالٍ لو بقي أي `localhost` في المخرجات.

---

## 7) كيف بُنيت `site-mysql` (للمرجع)

نُفّذت مرة واحدة بتاريخ 2026-08-19:

1. إنشاء قاعدة `clinic` على MariaDB 10.4 بترميز `utf8mb4`
2. نسخ `site/` إلى `site-mysql/` بعد حذف `wp-content/database` و`wp-content/db.php` وإضافة `sqlite-database-integration`
3. تعديل `wp-config.php`: `DB_NAME=clinic`، `DB_HOST=127.0.0.1:3307`، `DB_CHARSET=utf8mb4`، `DB_COLLATE=utf8mb4_unicode_ci` — مع الإبقاء على نفس المفاتيح الأمنية و`$table_prefix = 'wp_'`
4. `wp core install` لإنشاء بنية الجداول، ثم تفعيل الإضافات الأربع لإنشاء جداول Fluent Forms وActionScheduler
5. نسخ محتوى الجداول الـ24 كلها من SQLite إلى MySQL، والتحقق من تطابق عدد الصفوف
6. حذف `sqlite-database-integration` من `active_plugins`، وضبط `WPLANG = ar` (كانت غير مضبوطة محليًا — بدونها لا يعمل RTL)
7. `wp core update --version=7.0.4` لمطابقة نسخة الاستضافة

**تحققات أُجريت على المخرجات:**

- الجداول الـ24 متطابقة الصفوف بين SQLite وMySQL
- استُورد `clinic-db.sql` في قاعدة اختبار نظيفة بدون أي خطأ
- 53 خيارًا متسلسلًا و70 صف `postmeta` متسلسل — لا يوجد أي تلف بعد استبدال الروابط
- كل الصفحات المنشورة الـ11 ترجع 200 محليًا، وصفحات الأطباء المفردة تعمل، والنموذج يُعرض

---

## 8) لماذا ضاع الشغل سابقًا — تجنّبه

الموقع كان على `/wordpress/` ثم ظهر تثبيت جديد (7.0.4) على جذر الدومين مع مسح كل المحتوى. لم يكن هذا فعلًا عاديًا — يا إما الاستضافة أعادت التثبيت أو ثبّته أحد يدويًا.

قبل اعتبار الرفع منتهيًا:

- [ ] اعرف سبب المسح من دعم الاستضافة
- [ ] فعّل نسخًا احتياطية تلقائية من cPanel
- [ ] بعد كل تعديل مهم على الاستضافة، صدّر قاعدة البيانات واحتفظ بها محليًا
